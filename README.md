# Users Audit Logging System

Система логирования изменений и ежедневного экспорта данных пользователей в PostgreSQL.

## 📋 Описание проекта

Механизм аудита изменений в таблице users с автоматическим ежедневным экспортом в CSV.

## ⚙️ Функционал системы

- **Логирование изменений**: Триггер на таблице users фиксирует изменения в users_audit
- **Экспорт данных**: Ежедневная выгрузка данных за предыдущий день в CSV
- **Расписание задач**: `pg_cron` выполняет ежедневный экспорт в 3:00
- **Хранение**: Файлы сохраняются в Docker-контейнере по пути `/tmp/users_audit_export_YYYYMMDD_HH24MI.csv`

## 🛠️ Технологический стек

- PostgreSQL 15+
- Расширение `pg_cron`
- Docker

  ## 🗂️ Структура проекта
`users_audit_system.sql` - основной SQL-скрипт с реализацией

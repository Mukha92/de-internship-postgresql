# Users Audit Logging System

Система логирования изменений и ежедневного экспорта данных пользователей в PostgreSQL.

## 📋 Описание проекта

Проект реализует механизм аудита изменений в таблице `users` с автоматическим ежедневным экспортом свежих данных в CSV-файлы.

## ⚙️ Функционал системы

- **Логирование изменений**: Триггер на таблице `users` фиксирует изменения ключевых полей (все изменения фиксируются в таблице аудита `users_audit`)
- **Экспорт данных**: Функция выгружает данные за предыдущий день в CSV файл
- **Расписание задач**: `pg_cron` выполняет ежедневный экспорт в 3:00
- **Хранение**: Результаты сохраняются в Docker-контейнере по пути `/tmp/users_audit_export_YYYYMMDD_HH24MI.csv`

## 🛠️ Технологический стек

- PostgreSQL 15+
- Расширение `pg_cron`
- Docker
